CREATE TABLE predictions (symbol VARCHAR(32) NOT NULL, datetime VARCHAR(32) NOT NULL, open VARCHAR(32) NOT NULL, high VARCHAR(32) NOT NULL, low VARCHAR(32) NOT NULL, close VARCHAR(32) NOT NULL, volume VARCHAR(32) NOT NULL, adjusted VARCHAR(32) NOT NULL, id int(11) NOT NULL AUTO_INCREMENT, PRIMARY KEY(id));

